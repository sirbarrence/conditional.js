{
  "name": "conditional.js",
  "version": "0.0.1",
  "description": "Conditionally load RequireJS dependencies based on runtime configuration.",
  "scripts": {
    "test": "grunt test"
  },
  "author": "Barry Simpson",
  "license": "MIT",
  "devDependencies": {
    "grunt": "^0.4.5",
    "grunt-contrib-connect": "^0.8.0",
    "grunt-contrib-jasmine": "^0.6.5",
    "grunt-contrib-requirejs": "^0.4.4",
    "grunt-template-jasmine-requirejs": "^0.2.0"
  }
}
